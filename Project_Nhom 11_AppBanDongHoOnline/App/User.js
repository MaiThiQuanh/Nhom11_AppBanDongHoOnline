import React, {Component} from 'react';
import {View, Text, FlatList, StyleSheet, Image, TextInput, TouchableOpacity, Button} from 'react-native';

export default class User extends Component{
    render()
    {
        return(
            <Text>Đây là trang hồ sơ</Text>
        );
    }
}